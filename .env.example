FIREBIRD_HOST=127.0.0.1
FIREBIRD_PORT=3050
FIREBIRD_DATABASE=C:\\dados\\SEU_BANCO.fdb
FIREBIRD_USER=SYSDBA
FIREBIRD_PASSWORD=masterkey
FIREBIRD_CHARSET=WIN1252
FIREBIRD_ROLE=
# Opcional: sobrepor a query padrão de produtos
# Use ? como placeholder para parâmetros: (termo, termo)
# Exemplo:
# SQL_PRODUCT_QUERY=SELECT FIRST 50 CODIGO, DESCRICAO, PRECO, ESTOQUE FROM PRODUTOS WHERE CODIGO = ? OR UPPER(DESCRICAO) CONTAINING ? ORDER BY DESCRICAO
